import { Box } from '@mui/material';
import styled from '@emotion/styled';

export const CommunityPostWrapper = styled(Box)`
  display: flex;
  flex-direction: column;
  gap: 2rem;
  padding: 1rem 1rem 1rem;
`;
